<UserControl x:Class="psdPH.SpecialControls.AlignmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:psdPH.SpecialControls">
    <UserControl.Resources>
        <local:IsSelectedToBrushConverter x:Key="IsSelectedToBrushConverter"/>
    </UserControl.Resources>
    <Grid>
        <ItemsControl ItemsSource="{Binding AlignmentOptions}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Grid.Row" Value="{Binding Row}"/>
                    <Setter Property="Grid.Column" Value="{Binding Column}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.SelectAlignmentCommand, 
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding Alignment}"
                            ToolTip="{Binding DisplayName}"
                            Background="{Binding IsSelected, 
                                     Converter={StaticResource IsSelectedToBrushConverter}}"
                            Width="{Binding Dimension}"
                            Height="{Binding Dimension}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="ToolTipService.InitialShowDelay" Value="100"/>
                                <Setter Property="ToolTipService.BetweenShowDelay" Value="500"/>
                                <Setter Property="ToolTipService.ShowDuration" Value="1000"/>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>