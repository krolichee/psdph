<Window x:Class="psdPH.PsdTemplateDropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drag and Drop File Example" Height="350" Width="525"
        AllowDrop="True"
        Drop="Window_Drop">
    <Grid>
        <Grid Margin="0,65,0,40">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FFE6E6E6" Offset="1"/>
                    <GradientStop Color="White"/>
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock x:Name="labelDrop" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   FontSize="20" Text="Перетащите файл .psd"/>
            <TextBox x:Name="projectNameTB" HorizontalAlignment="Left" Margin="183,-33,0,0" TextWrapping="Wrap" Text="Новый проект" VerticalAlignment="Top" Width="120"/>
            <Label Content="Ввод названия проекта" HorizontalAlignment="Left" Margin="40,-41,0,0" VerticalAlignment="Top"/>

        </Grid>
        <Button Content="Ok" HorizontalAlignment="Left" Height="25" Margin="450,305,0,0" VerticalAlignment="Top" Width="45" Click="Button_Click"/>
    </Grid>

</Window>